import React from 'react';
import styled from 'styled-components';

const SetListPageLayout = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  min-height: 100vh;
  position: relative;
`;

const ImgSubmit = (userImg) => {
  return <SetListPageLayout></SetListPageLayout>;
};

export default ImgSubmit;
